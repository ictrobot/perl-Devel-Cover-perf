# Example application

This is a synthetic, LLM-generated benchmark fixture — not a real application.
It exists to provide enough modules and dependencies to make `Devel::Cover`
overhead measurable. See the top-level [README](../README.md).

**This code is insecure. Do not copy or reuse it.** It contains insecure
password handling, non-cryptographic randomness, and no input validation or
access control.
